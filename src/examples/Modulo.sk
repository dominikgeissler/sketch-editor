bit[8] x = {1,0,1,0,1,0,1,0};

harness void main() {
<<<<<<< HEAD
  // One can change the expected bits
  // to get some interesting results
=======
>>>>>>> Added color rendering in output, example from my seminar paper
	bit[4] expected = {1,0,1,1};
	assert doSth(x) == expected;
}

bit[4] doSth(bit[8] in) {
	bit[4] ret = 0;
	int bitC = 0;
	for(int i = 0; i < 8; i++) {
		if(i % ?? == ??) {
			ret[bitC] = in[i];
			bitC = bitC + 1;
		}
	}
	return ret;
}
